apiVersion: apps/v1
kind: Deployment
metadata:
  name: pihole
  namespace: infra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pihole
  template:
    metadata:
      labels:
        app: pihole
    spec:
      containers:
      - name: pihole
        image: pihole/pihole:latest
        ports:
        - containerPort: 53
          name: dns-tcp
          protocol: TCP
        - containerPort: 53
          name: dns-udp
          protocol: UDP
        - containerPort: 80
          name: http
          protocol: TCP
        env:
        - name: TZ
          value: "Europe/Brussels"
        - name: WEBPASSWORD
          value: "monkey1"
        - name: ServerIP
          value: "192.168.1.120"
        - name: VIRTUAL_HOST
          value: "YOUR_VIRTUAL_HOST"
        - name: FTLCONF_LOCAL_IPV4
          value: "192.168.1.120"
        volumeMounts:
        - name: pihole-etc
          mountPath: /etc/pihole
        - name: pihole-dnsmasq
          mountPath: /etc/dnsmasq.d
      volumes:
      - name: pihole-etc
        persistentVolumeClaim:
          claimName: pihole-etc-pvc
      - name: pihole-dnsmasq
        persistentVolumeClaim:
          claimName: pihole-dnsmasq-pvc
